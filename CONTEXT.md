# WasmSharp2

WasmバイナリをC#から扱うランタイムと、その公式適合検証で用いる概念と用語。

## 用語

### モジュールと値

**リソースの同一性**:
複数のimport/exportが、同じメモリ・テーブル・globalの実体を指しているという関係。
_避ける表現_: 値の一致、同じ内容のコピー。

### 処理段階と結果

**Decode（デコード）**:
Wasmバイナリの構文を解釈してモジュール定義を得る段階。
_避ける表現_: 4段階全体を意味するロード。

**Validate（検証）**:
デコードされたモジュール定義が、対象とするWasm仕様の型や構造の規則を満たすか判定する段階。
_避ける表現_: テスト実行、リンク確認。

**Instantiate（インスタンス化）**:
検証された定義のimportを具体的な外部要素へ結びつけ、定義を割り当て・初期化してstartを実行する段階。
_避ける表現_: 検証、単なるオブジェクト生成。

**Invoke（呼び出し）**:
具体的な引数を関数へ渡し、実行結果を得る段階。
_避ける表現_: モジュールのロード。

**Wasm実行コンテキスト**:
最外側の関数呼び出しまたはstart実行から、その終了までの呼び出し連鎖をまとめる単位。呼び出し深さと、開始時に決まった上限を共有する。
_避ける表現_: WasmInstance、個々のInvoke。

**実行ポリシー**:
インスタンスが新たなWasm実行コンテキストを開くときに用いる実行上限の設定。
_避ける表現_: 呼び出されるたびに適用する上限、インスタンス固有の再帰深さ。

**リンク不成立**:
必要なimportが見つからない、または提供された外部要素の型が合わないため接続できないこと。
_避ける表現_: startのtrap、型検証失敗。

**trap**:
Wasmの実行規則で定められた異常により、その実行が中断すること。関数呼び出しだけでなく、インスタンス化中の初期化やstartでも生じる。
_避ける表現_: あらゆる失敗、リンク不成立、未実装。

**exhaustion（実行資源の上限到達）**:
ランタイムが管理する実行資源の上限に達したため、その実行を継続できなくなること。
_避ける表現_: Wasmの仕様上のtrap、未実装。

### 公式適合検証

**検証プロファイル**:
検証の前提となるWasm仕様の版と、機能ごとの有効・無効の組み合わせ。
_避ける表現_: 現在の実装済み機能一覧。

**公式テスト素材（corpus）**:
固定した公式テストの入力集合と、その検証に使用する変換済み素材。
_避ける表現_: その時点の全proposal、mainの全ファイル。

**spectest**:
公式テストがimportするために定めた、ホスト関数とリソースのまとまり。
_避ける表現_: ランタイムのテスト専用機能。

**成立（passed）**:
公式テストの期待した結果が成立した状態。必要なセットアップや単独actionでは、処理が正常に完了した状態。
_避ける表現_: 素材の変換成功、Core 2.0全体への準拠。

**不一致（failed）**:
公式テストで判定した結果が、期待する結果と異なる状態。
_避ける表現_: 未対応、検証処理の異常、依存による未実行。

**ランタイム未対応（runtime_unsupported）**:
ランタイムの未実装機能に遭遇したため、公式テストの判定を完了できない状態。この分類だけで入力が仕様上有効と証明されたことにはならない。
_避ける表現_: 検証不成立、ランナー未対応、対象外。

**ランナー未対応（runner_unsupported）**:
公式テストのcommandや期待値の比較にランナーが対応しておらず、判定を完了できない状態。
_避ける表現_: ランタイム未対応、検証処理の異常。

**ランナー異常（runner_error）**:
素材の変換失敗、JSONの破損、素材の欠落などにより、公式テストの検証処理が成立しない状態。
_避ける表現_: 未対応、期待したWasmの失敗。

**対象外（out_of_scope）**:
選定した仕様範囲や入力形式の約束に含めていない検証対象。
_避ける表現_: 未実装、合格。

**前提不成立（blocked）**:
依存するmoduleの生成やregisterなどが成立せず、その結果を必要とする後続commandを実行できない状態。
_避ける表現_: 当該command自身の未対応、合格。

**変換baseline**:
固定した公式入力の変換結果と素材の識別情報を、再生成との比較基準として保存したもの。
_避ける表現_: ランタイムの適合結果、実行結果baseline。

**実行結果baseline**:
同じ公式ケースを再実行したときの比較基準として保存した、各commandの結果。
_避ける表現_: 正しい期待値、全件合格の証明、変換baseline。
