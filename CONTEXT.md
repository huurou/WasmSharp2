# WasmSharp2

WasmバイナリをC#から扱うランタイムと、その公式適合検証で用いる概念と用語。

## 用語

### モジュールと値

**WasmModule（モジュール）**:
型、関数、リソース、import/export等を記述する静的なモジュール定義。
_避ける表現_: 実行インスタンス、ロード済みインスタンス。

**WasmInstance（インスタンス）**:
モジュール定義を具体的な関数・リソースへ結びつけた実行時の実体。同じ定義から生成しても、インスタンスとその状態は同じものとは限らない。
_避ける表現_: 静的モジュール。

**WasmValue（Wasm値）**:
Wasmの型と、それに対応する数値・ベクトル・参照を表す値。
_避ける表現_: CLR引数、汎用object値。

**WasmFunctionType（関数型）**:
関数の引数型の列と結果型の列からなる型。
_避ける表現_: delegate型。

**WasmFunction（関数）**:
Wasmの関数型を持つ呼び出し対象。モジュールで定義された関数とホスト関数がある。

**WasmHostModule（ホストモジュール）**:
ホストがWasmのimportへ提供する、名前付きの関数やリソースのまとまり。

**リソースの同一性**:
複数のimport/exportが、同じメモリ・テーブル・globalの実体を指しているという関係。
_避ける表現_: 値の一致、同じ内容のコピー。

### 処理段階と結果

**Decode（デコード）**:
Wasmバイナリの構文を解釈してモジュール定義を得る段階。
_避ける表現_: 4段階全体を意味するロード。

**Validate（検証）**:
デコードされたモジュール定義が、対象とするWasm仕様の型や構造の規則を満たすか判定する段階。
_避ける表現_: テスト実行、リンク確認。

**Instantiate（インスタンス化）**:
検証された定義のimportを具体的な外部要素へ結びつけ、定義を割り当て・初期化してstartを実行する段階。
_避ける表現_: 検証、単なるオブジェクト生成。

**Invoke（呼び出し）**:
具体的な引数を関数へ渡し、実行結果を得る段階。
_避ける表現_: モジュールのロード。

**リンク不成立**:
必要なimportが見つからない、または提供された外部要素の型が合わないため接続できないこと。
_避ける表現_: startのtrap、型検証失敗。

**trap**:
Wasmの実行規則で定められた異常により、その実行が中断すること。関数呼び出しだけでなく、インスタンス化中の初期化やstartでも生じる。
_避ける表現_: あらゆる失敗、リンク不成立、未実装。

**未実装（unsupported）**:
対象とする仕様の機能について、ランタイムの実装が不足している状態。この分類だけで入力が仕様上有効と証明されたことにはならない。
_避ける表現_: 検証不成立、対象外。

### 公式適合検証

**検証プロファイル**:
検証の前提となるWasm仕様の版と、機能ごとの有効・無効の組み合わせ。
_避ける表現_: 現在の実装済み機能一覧。

**公式テスト素材（corpus）**:
固定した公式テストの入力集合と、その検証に使用する変換済み素材。
_避ける表現_: その時点の全proposal、mainの全ファイル。

**spectest**:
公式テストがimportするために定めた、ホスト関数とリソースのまとまり。
_避ける表現_: ランタイムのテスト専用機能。

**対象外（out_of_scope）**:
選定した仕様範囲や入力形式の約束に含めていない検証対象。
_避ける表現_: 未実装、合格。

**ツール不成立（tool_error）**:
変換やテストの実行準備が成立せず、ランタイムの仕様上の結果を確認できていない状態。
_避ける表現_: ランタイムの未実装、Wasmのtrap。
